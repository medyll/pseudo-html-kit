# PRD — v0.5.0 (CSS 2026 theme)

## Overview
v0.5.0 focuses on modern CSS-powered components that leverage the newest stable CSS 2026 primitives (Scroll Snap, anchor positioning, `text-wrap: balance`, and the emerging `if()` function). The goal is to deliver highly-performant, low-JS components that are progressive-enhancement friendly and accessible by default.

## Goals
- Implement a performant, accessible carousel component using CSS Scroll Snap (carousel-pk).
- Provide an accessible date-picker (date-picker-pk) with graceful enhancement.
- Deliver a color-swatch component and refine text wrapping (`text-wrap: balance`) visuals across components.
- Run a CSS `if()` pass to simplify fallbacks and document patterns.

## Scope (MVP)
- carousel-pk (Must) — CSS Scroll Snap with keyboard, focus management, and SSR-friendly markup.
- date-picker-pk (Should) — native input enhancement + accessible calendar popover fallback.
- color-swatch-pk (Could) — accessible color swatches with ARIA and keyboard selection.
- Tests, examples, and documentation for each component.

## Success criteria
- All components render and function with zero JS errors in supported browsers.
- Unit coverage for new components >= 100% for client code paths.
- Storybook-like demo pages and docs updated.

## Initial backlog (Sprint 12)
- S12-01 — Carousel: CSS Scroll Snap (5 pts) — Must
- S12-02 — Date Picker: accessible calendar (3 pts) — Should
- S12-03 — Color Swatch: accessible swatch picker (3 pts) — Could

## Timeline
Sprint 12 (v0.5.0 kickoff): implement S12-01..S12-03, tests, and docs.

*Generated by bmad-master next --auto on 2026-03-07.*
