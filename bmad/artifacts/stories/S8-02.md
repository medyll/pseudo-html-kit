# S8-02 — Vitest unit tests: Molecules (16 components)

**Sprint:** Sprint 08 — Quality, DX & Framework Reach
**Epic:** A – Unit Tests
**Points:** 7
**Priority:** Must
**Status:** done

---

## Description

Write Vitest unit tests for all 16 molecule-layer components in `src/pseudo-assets/components/molecules/`. Same pattern as S8-01.

**Components in scope:**
breadcrumb, card, card-media, dropdown, form-field, list-item, menu-item, modal, notification, pagination, price-tag, product-tile, search-bar, tab-bar, tooltip, user-info

---

## Acceptance Criteria

- [x] One `describe` block per component (16 total)
- [x] Each block covers: renders, DOM structure, slots, attribute reflection
- [x] Tests read actual HTML files via `fs.readFileSync`
- [x] `vitest run tests/molecules.client.test.js` passes — 53 tests, 0 failures

---

## Implementation Notes

**Date:** 2026-03-03
**Files changed:**
- `tests/molecules.client.test.js` — new; 16 describe blocks, 53 tests

**Notable decisions:**
- Same helper pattern as atoms (`registerAndInit`, `flush`, `mockFetch`).
- Modal ARIA attributes (role=dialog, aria-modal) explicitly verified.
- Tab-bar active state (aria-selected) verified.
