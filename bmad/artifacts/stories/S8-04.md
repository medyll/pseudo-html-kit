# S8-04 — Automated a11y audit (axe-core via Playwright)

**Sprint:** Sprint 08 — Quality, DX & Framework Reach
**Epic:** B – A11y
**Points:** 5
**Priority:** Must
**Status:** done

---

## Description

Run an automated accessibility audit against all 46 pseudo-kit components using axe-core via `@axe-core/playwright`. Produce a machine-readable and human-readable report stored as `bmad/artifacts/a11y-audit-{date}.md`. Surface Critical and Serious violations for S8-05 to fix.

---

## Acceptance Criteria

- [x] Playwright a11y config created (`playwright.a11y.config.js`) — serves `src/` on port 3001
- [x] A11y harness page created (`src/a11y-audit.html`) — all 46 components registered + resolved before scan
- [x] Test suite (`tests/a11y-audit.a11y.e2e.js`) — full page scan + per-layer scans (atoms, molecules, organisms)
- [x] Report saved to `bmad/artifacts/a11y-audit-{date}.md` on every test run
- [x] `pnpm test:a11y` command added to `package.json`

---

## Implementation Notes

**Date:** 2026-03-03
**Files created:**
- `playwright.a11y.config.js` — Playwright config for a11y tests (port 3001, Chromium only)
- `src/a11y-audit.html` — harness page; registers all 46 components, waits for `data-pk-resolved` on all before setting `window.__pkA11yReady = true`
- `tests/a11y-audit.a11y.e2e.js` — 4 tests: full page scan + atoms/molecules/organisms layer scans
- `bmad/artifacts/a11y-audit-2026-03-03.md` — generated report

**Initial audit results (at implementation time):**
- The fixture was designed with proper ARIA patterns (aria-label forwarding, role="list", etc.)
- Result: 0 Critical, 0 Serious violations on first run
- Moderate: 1 (landmark-unique — cart-summary aside, logged as backlog)
- Minor: 1 (empty-heading — profile-card h2 with pk-slot, logged as backlog)
