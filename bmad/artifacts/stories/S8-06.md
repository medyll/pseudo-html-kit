# S8-06 — JSDoc annotations: Atoms + Molecules

**Sprint:** Sprint 08 — Quality, DX & Framework Reach
**Epic:** C – JSDoc
**Points:** 4
**Priority:** Should
**Status:** done

---

## Description

Upgrade component HTML headers for all atoms (17) and molecules (16) to use rich JSDoc-style annotations (`@prop {type}`, `@slot name - description`).

- **Atoms**: Already done (rich style verified).
- **Molecules**: Currently use the simple `@props`/`@slots` pseudo-html format → upgrade to rich `@prop {type} [name] - description` / `@slot name - description`.

Components in scope (molecules only):
breadcrumb-pk, card-media-pk, card-pk, dropdown-pk, form-field-pk, list-item-pk, menu-item-pk, modal-pk, notification-pk, pagination-pk, price-tag-pk, product-tile-pk, search-bar-pk, tab-bar-pk, tooltip-pk, user-info-pk

---

## Acceptance Criteria

- [ ] All 16 molecule headers use `@prop {type} [name] - description` syntax
- [ ] Optional props wrapped in `[brackets]`, required props without
- [ ] All slots documented with `@slot name - description`
- [ ] `@event` added where the component header references event-emitting behaviour
- [ ] No functional change to component logic or styling

---

## Technical Notes

- Rich format reference: `atoms/button-pk.html` (already correct)
- Enums use `{'a'|'b'|'c'}` JSDoc type syntax
- Arrays use `Array<type>` or `{type}[]`
- Objects use `{{field: type, field2: type}}`

## Definition of Done

- [ ] All 16 molecule `.html` files updated on disk
- [ ] `node --test` and `vitest run` still pass
