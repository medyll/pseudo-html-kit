# S8-08 — `pseudo-kit-react` package scaffold

**Sprint:** Sprint 08 — Quality, DX & Framework Reach
**Epic:** D – React
**Points:** 3
**Priority:** Should
**Status:** done

---

## Description

Create the `pseudo-kit-react` workspace package at `src/pseudo-kit-react/`. This is a thin React adapter that allows using pseudo-html-kit components inside a React 18+ app.

---

## Acceptance Criteria

- [ ] `src/pseudo-kit-react/package.json` created with:
  - `"name": "pseudo-kit-react"`
  - `"version": "0.1.0"`
  - `"type": "module"`
  - `peerDependencies`: `react >= 18`, `react-dom >= 18`
  - `exports`: `"."` → `./src/index.js`
- [ ] `src/pseudo-kit-react/src/index.js` — barrel export
- [ ] `src/pseudo-kit-react/README.md` — minimal usage docs
- [ ] `pnpm-workspace.yaml` updated to include `src/pseudo-kit-react`
- [ ] `pnpm install` succeeds

---

## Technical Notes

- No build step — pure ESM
- Client-side only in v0.3.0; SSR deferred to v0.4.0
- peerDep on `pseudo-stack` (the main package) for `PseudoKit`

## Dependencies

- None (standalone scaffold)

## Definition of Done

- [ ] Package installable via `pnpm install` in workspace
- [ ] Barrel export works: `import { useComponent } from 'pseudo-kit-react'`
