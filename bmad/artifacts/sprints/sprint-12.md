# Sprint 12 — v0.5.0 (CSS 2026 theme)

Status: in progress

## Theme
CSS 2026: carousel (Scroll Snap), date-picker, color-swatch, text-wrap balance, CSS `if()` pass.

## Stories
- S12-01 — Carousel: CSS Scroll Snap (5 pts) — Must — implement `carousel-pk`, keyboard support, SSR-friendly markup, demo + tests
- S12-02 — Date Picker: accessible calendar (3 pts) — Should — implement `date-picker-pk` with popover calendar fallback + tests
- S12-03 — Color Swatch: accessible swatch picker (3 pts) — Could — implement `color-swatch-pk` with ARIA + keyboard

## Definition of Done
- Component HTML/CSS files added to pseudo-assets
- Unit tests (vitest + happy-dom) covering rendering + interactions
- Demo page and README updates

*Created by bmad-master next --auto: 2026-03-07.*
